# ubuntu docker in docker

## up

- `docker-compose up -d`

## connect docker

- `docker-compose exec srv bash`

## child docker-compose

- `docker-compose up -d`
- `docker-compose exec srv bash`

...

